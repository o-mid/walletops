DATABASE_URL=postgres://walletops:walletops@localhost:5432/walletops?sslmode=disable
JWT_SECRET=dev-only-change-me
WEBHOOK_SECRET=dev-webhook-secret
AI_PROVIDER=mock
AI_API_KEY=
AI_BASE_URL=
HTTP_ADDR=:8080
# Local demo: keep events in processing long enough to see PENDING → PROCESSING in the app.
DEMO_PROCESS_DELAY_MS=4000

